% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{Disp2}
\alias{Disp2}
\title{Aggregate-Level Disproportionality Measures}
\usage{
Disp2(
  seed = 0,
  np = 3,
  nd = 1,
  ne = 100,
  dist = "lnorm",
  rate = 1/25000,
  mean = 10,
  sd = 1.2,
  max = 1e+05,
  phi = 20,
  votes_per_district = 1e+05,
  minTS = 3,
  maxTS = 20,
  jump = 1,
  threshold = 0,
  threshold_country = 0,
  start_C = 0.2,
  start_alpha = -0.2,
  model = TRUE,
  methods = c("dh", "sl", "msl", "hamilton", "hh", "ad", "imperiali", "danish"),
  formula_dist = "hh"
)
}
\arguments{
\item{seed, np, nd, ne, dist, rate, mean, sd, max, phi, votes_per_district}{Parameters passed to \code{simulate_E}.}

\item{minTS, maxTS, jump}{Integer: range and step for total seats.}

\item{threshold, threshold_country}{Numeric: electoral thresholds.}

\item{start_C, start_alpha}{Numeric: NLS starting values.}

\item{model}{Logical: if \code{TRUE}, fit exponential decay models.}

\item{methods}{Character vector: method codes to simulate. Defaults to
\code{c("dh", "sl", "msl", "hamilton", "hh", "ad", "imperiali", "danish")}.
Any divisor-method code accepted by \code{divisorMethods} (\code{"dh"},
\code{"sl"}, \code{"msl"}, \code{"danish"}, \code{"hsl"}, \code{"imperiali"},
\code{"hh"}, \code{"wb"}, \code{"jef"}, \code{"ad"}, \code{"hb"}) or
\code{"hamilton"} may be supplied; an unknown code raises an error.}

\item{formula_dist}{Character: method for inter-district seat allocation.}
}
\value{
A list with \code{summary} (combined disproportionality data),
  per-method model objects (if \code{model = TRUE}), and per-method
  raw simulation results.
}
\description{
Runs simulations across multiple apportionment methods and seat counts,
computing aggregate disproportionality indexes (GHI, LHI, SLI, ENPP, NPP).
Optionally fits \eqn{GHI \sim C \cdot e^{\alpha \cdot DM}} models.
}
